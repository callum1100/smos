# ── Authentication ──────────────────────────────────────
# Secret key for signing JWTs (must be at least 32 characters)
JWT_SECRET=your-secret-key-change-this-to-something-random

# Comma-separated list of allowed email addresses
# Leave empty to allow all emails (dev mode only!)
ALLOWED_EMAILS=callum@salesmachines.io,andreas@salesmachines.io,virgile@salesmachines.io,rene@salesmachines.io

# ── Email (Resend) ──────────────────────────────────────
# Get your API key at https://resend.com
# Leave empty for dev mode (magic links logged to console)
RESEND_API_KEY=
EMAIL_FROM=SMOS <noreply@salesmachines.io>

# ── App ─────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Database ────────────────────────────────────────────
# For production (Vercel Postgres / Neon), uncomment:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
